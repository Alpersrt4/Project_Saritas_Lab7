<Window x:Class="WpfUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lab 7 - WPF Interface" Height="450" Width="800">

	<Window.Resources>
		<Style TargetType="Button">
			<Setter Property="Background" Value="#2196F3"/>
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="FontSize" Value="14"/>
			<Setter Property="Margin" Value="5,10"/>
			<Setter Property="Padding" Value="10,5"/>
		</Style>
	</Window.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="200"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<Border Grid.Row="0" Grid.ColumnSpan="2" Background="#333" Padding="15">
			<TextBlock Text="Yusuf Alper Saritas (Юсуф Алпер Сариташ) - Lab 7 WPF App" Foreground="White" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center"/>
		</Border>

		<StackPanel Grid.Row="1" Grid.Column="0" Background="#f4f4f4">
			<TextBlock Text="Operations" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15" HorizontalAlignment="Center"/>
			<Button Name="BtnAdd" Content="Add" Click="BtnAdd_Click"/>
			<Button Name="BtnEdit" Content="Edit" Click="BtnEdit_Click"/>
			<Button Name="BtnDelete" Content="Delete" Click="BtnDelete_Click"/>
		</StackPanel>

		<DataGrid Name="MainDataGrid" Grid.Row="1" Grid.Column="1" AutoGenerateColumns="True" Margin="10" IsReadOnly="True" />
	</Grid>
</Window>